import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';

<Meta title="Guide|Customization" />

# Component customization

There are four ways to customise a component.

## Style all components via theming

This approach is the most recommended one.
Theming allows to decouple components and tokens.
Draw a picture, blah, blah

// ToDo

## Style one components with withStyles()

// ToDo

## Style all or some components via styled()

// some components:

CSS cascading example here

// one component:

const CustomButton = styled(Button).attr({classNamePrefix: 'Custom-Button'})` *** CSS cascading here *** `;

// ToDo
